import siteJson from '../data/site.json';
import type { SiteContent } from './types';

/** All user-editable copy and business data, committed in `src/data/site.json`. */
export const site: SiteContent = siteJson;
